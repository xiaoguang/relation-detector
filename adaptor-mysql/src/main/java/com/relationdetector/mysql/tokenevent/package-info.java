/**
 * MySQL token-event parser 子包。
 *
 * <p>CN: 本包提供 MySQL 生产 fallback parser 入口，选择 MySQL tolerant grammar 和
 * MySQL 方言 token-event builder。它不包含 full-grammer 版本化实现。
 *
 * <p>EN: MySQL token-event parser package. It provides production fallback
 * parser entry points selecting the MySQL tolerant grammar and MySQL
 * token-event builder. Versioned full-grammer implementation is not kept here.
 */
package com.relationdetector.mysql.tokenevent;
