/**
 * PostgreSQL token-event parser 子包。
 *
 * <p>CN: 本包提供 PostgreSQL 生产 fallback parser 入口，选择 PostgreSQL tolerant
 * grammar 和 PostgreSQL 方言 token-event builder。它不包含 full-grammer 版本化实现。
 *
 * <p>EN: PostgreSQL token-event parser package. It provides production fallback
 * parser entry points selecting the PostgreSQL tolerant grammar and PostgreSQL
 * token-event builder. Versioned full-grammer implementation is not kept here.
 */
package com.relationdetector.postgres.tokenevent;
