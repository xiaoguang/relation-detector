# 代码与设计对应审视报告

本文按当前代码实现逐环节对照设计文档，列出代码入口、设计章节、测试覆盖和差异状态。状态含义：

- `MATCHED`：代码、设计、测试一致。
- `DOC_UPDATED`：本轮已按当前代码更新设计/测试说明。
- `FOCUSED_GREEN`：实现与设计一致，并已通过对应 focused contract；发布级结论仍由完整验收清单给出。
- `PARTIAL`：主链路已经落地，但仍有已定位的实现缺口，不能宣称完全匹配。
- `REVIEW_NEEDED`：实现与设计有差异，或存在后续设计取舍，需要人工审视。
- `BACKLOG`：明确不属于当前已实现能力的扩展项，不影响已经冻结的契约。

## 当前收敛状态

下方具名 traceability 行是当前唯一收敛矩阵；不再维护无法回溯到 requirement、代码和测试的匿名
closure ID。`MATCHED` 只表示该行列出的实现、设计与测试相互一致；`PARTIAL` 必须
对应已定位且仍需实现的具名缺口。环境性 runtime smoke 与明确标为 `BACKLOG` 的能力不重新开启已完成项，
但也不能被用来掩盖新的实现差异。

## 代码注释同步范围

生产代码的 package、命名类型和方法级结构说明已同步到代码侧；SPI v6、live collector、preflight 和
profiling outcome 的关键入口 Javadoc 已同步。架构测试使用 JDK compiler/doc-tree API 检查全部手写
生产 package 的中英双语职责契约，以及生产类型和大型编排方法的具体责任说明。后续修改必须遵守以下规则：

- 每个生产 package 的 `package-info.java` 必须以中英双语说明 responsibility、inputs、outputs、upstream/downstream 和 forbidden responsibility。架构测试验证这些语义类别、最小具体内容及禁用模板；描述是否准确仍需代码评审。
- 顶层 public/protected 手写类型及编排职责类的类级 Javadoc 必须使用 `CN:` / `EN:` 具体说明职责和禁止边界。
- 编排职责类中有效代码超过 40 行的非 override 方法必须说明输入效果、输出/副作用与失败边界；简单 getter、record accessor 和小工具方法不在自动门禁范围内。

若后续新增生产 package、核心类或跨链路调用，必须同步更新对应注释、Phase 6 详细设计和本代码与设计对应表。
`DialectGrammarArchitectureTest` 与 `SemanticDocumentationArchitectureTest` 当前会拒绝缺少双语 package
契约、public/protected 顶层类型、已登记 suffix 编排类、大方法 Javadoc，或使用已禁止通用模板的手写
生产代码。编排集合同时覆盖 `Assembly/Factory/Assembler/Resolver/Index` 以及
`Executor/Runner/Scheduler/Loader/Normalizer/Dispatcher/Selector`；新后缀门禁命中的
`ScanTaskExecutor`、`SingleScanRunner`、`BatchScheduler`、`BatchManifestLoader`、
`DdlRelationParserRunner` 大方法和 `SemanticSectionNormalizer` 已补齐具体双语职责。
门禁不覆盖 generated、accessor 或显而易见的小 helper，也不会代替语义评审。

| 环节 | 代码入口 | 设计章节 | 测试覆盖 | 状态 | 差异 / 审视点 |
| --- | --- | --- | --- | --- | --- |
| CLI config / argument parsing | `cli.Main.MainCommand`, `SimpleYamlConfigLoader`, `SingleScanRunner`, `BatchCommand` | `phase-06 Parser mode 和 profile 选择`, `phase-08 输出与配置` | `ParserConfigRemovalTest`, `CliErrorCodeContractTest`, `BatchCommandIntegrationTest`, `BatchSchedulerTest`, `MySqlMetadataCollectorFactsTest` | MATCHED | typed配置异常保持稳定错误码；MySQL `database.catalog`与legacy `database.schema`冲突在scope canonicalization阶段抛出固定脱敏的`LiveSourceConfigurationException`。direct API原样传播，single CLI和batch case统一归类为`CONFIG_FORMAT_ERROR`，且失败早于连接创建和首条JDBC查询。 |
| adaptor discovery | `cli.AdaptorRegistry`, `core.adaptor.AdaptorContractValidator` | `phase-03 Adaptor SPI`, `phase-06 full-grammar module 注入链` | `AdaptorApiVersionTest`, `CommonAdaptorRegistryTest`, `CliEndToEndGoldenTest` | FOCUSED_GREEN | 通过 Java SPI 发现 `DatabaseAdaptor`；registry 与 direct `ScanEngine` 复用同一完整 validator 检查 SPI v6、database type 和显式 adaptor id，CLI 边界将 validator failure 包装为既有 `AdaptorException`。v5 外部插件在 registry 阶段被明确拒绝并提示重新编译；v6 `AdaptorParsers.scriptFramer()` 提供必需的 `DialectScriptFramer`，full-grammar module 由独立 `ServiceLoader<FullGrammarDialectModule>` 注入。 |
| ScanEngine source orchestration | `core.scan.ScanEngine.scan`, `core.input.ScanInputPathResolver`, `SourceCollectorPipeline`, `StatementParsePipeline`, `core.execution.StatementExecutionService` | `phase-06 ScanEngine 总编排`, `phase-08 execution 配置` | `FinalScanContractTest`, `ScanInputPathResolverTest`, `ScanEngineDatabaseDdlSourceTest`, `ScanEngineDiagnosticsTest`, `ScanExecutionParallelismTest`, `CliEndToEndGoldenTest`, `CorrectnessFixtureRunnerTest` | FOCUSED_GREEN | `ScanConfig.resolve(baseDirectory)` 是 `files + paths + include` 的唯一展开入口；CLI 使用配置文件父目录，direct API 无参入口使用当前工作目录。运行态只消费稳定排序、规范绝对路径且去重的 `*Files`，missing、non-regular 或 unreadable 输入会在 scan 前明确失败。 |
| SQL/DDL parser selection | `core.parser.runtime.ParserBundleSelector`, `ParserBundle`, `ParserSelectionResult` | `phase-06 Parser mode 和 profile 选择` | `ParserBundleSelectorTest`, `SqlRelationParserRunnerTest`, `DdlRelationParserRunnerTest`, `ParserConfigRemovalTest`, `CliEndToEndGoldenTest` | MATCHED | `auto/full-grammar/token-event`选择策略由一个bundle统一完成；只有外部full parser调用抛出的普通runtime failure使用固定warning回退。parser返回后，selection attributes装配和core校验位于fallback catch之外；null result/null attributes及parser直接抛出的`AdaptorContractException`均保持契约错误且禁止fallback。 |
| SQL runner policy 职责 | `core.parser.runtime.SqlRelationParserRunner`, `core.execution.StatementExecutionService` | `phase-06 Scan/SQL 调用链` | `SqlRelationParserRunnerTest`, `DialectGrammarArchitectureTest`, correctness execution service tests | MATCHED | parser selection 与 typed log-noise policy 保留在实际需要配置的 bundle/scan 路径。runner 直接把原始 statement 交给 parser；空的 policy-attribute helper 和 direct SQL execution 的无效 config overload 已删除。fallback parser 使用 detached context，candidate 与 warning 全批校验成功后才转发。 |
| token-event SQL parse | `core.parser.tokenevent.CommonTokenEventStructuredSqlParser`, `TypedDialectTokenEventStructuredSqlParser`, dialect parse-tree visitors | `phase-06 SQL / DML token-event 链路` | `CommonTokenEventStructuredSqlParserTest`, `CommonTokenEventStructuredSqlParserAdditionalTest`, dialect boundary tests, correctness fixtures | MATCHED | legacy `TokenEventStructuredSqlParser` wrapper 已删除；common parser直接拥有生命周期，方言 parser 继承 typed lifecycle，生产事件来自 typed structural grammar + visitor。 |
| full-grammar SQL parse | MySQL `fullgrammar/common` + `v5_7|v8_0` parsers、PostgreSQL `fullgrammar/common` + `v16|v17|v18` parsers、Oracle `fullgrammar/common` + `v12c|v19c|v21c|v26ai` `INCOMPLETE_VERSIONED` parsers、SQL Server `fullgrammar/common` + `v2016|v2017|v2019|v2022|v2025` parsers | `phase-06 SQL / DML full-grammar 链路`, `phase-04`, `phase-05`, `phase-09`, `phase-10` | `CorrectnessFixtureRunnerTest`, `FullGrammarSqlBehaviorTest`, versioned correctness fixtures, adaptor parser architecture tests | DOC_UPDATED | 运行时选中 profile 后 full-grammar 是 primary parser；它只消费 typed generated context，不使用 rule name、reflection 或 raw SQL regex 推断结构；默认测试不再断言 native/delegate/bridge 过程属性，也不再用 token-event baseline 做跨 parser 兜底。PostgreSQL routine body由`postgres.plpgsql.v16|v17|v18`同版本shell解析，token-event使用独立`postgres.plpgsql.tokenevent` shell。Oracle当前是`INCOMPLETE_VERSIONED` generated parser，不再桥接token-event，更广泛的Oracle官方语法覆盖是backlog。 |
| dialect script framing | `core.script.StructuredScriptFramer`, five `*ScriptSlicePlanner`, `ScriptFileExtractor`, dialect `*ScriptFramer` | `phase-03 DialectScriptFramer`, `phase-04`, `phase-05`, `phase-09`, `phase-10` | dialect script framer tests, architecture test | MATCHED | MySQL `DELIMITER`、PostgreSQL dollar quote、Oracle single-line slash、SQL Server single-line `GO` 和 common compound object framing 分别由独立 planner 处理；`StructuredScriptFramer` 只编排 marker、slice 选择及 statement/provenance 装配。 |
| relationship extraction / naming evidence | `core.relation.StructuredRelationshipExtractor`, `RelationshipAliasResolver`, `RelationshipCandidateFactory`, `RelationshipMerger`; `core.naming.NamingEvidenceExtractor`, `NamingRuleEngine`, `NamingRuleSet`, `NamingEvidenceMerger`, `NamingMatchEvidenceEnhancer` | `phase-02 SQL 谓词关系守卫`, `phase-06 Relationship 抽取` | `CorrectnessFixtureRunnerTest`, `CorrectnessNamingEvidenceGoldenTest`, `NamingEvidenceExtractorTest`, `NamingRuleEngineCatalogIdentityTest`, `RelationshipMergerEvidenceAggregationTest`, `FinalEvidenceContractTest` | MATCHED | `NamingRuleEngine` 是唯一命名启发式边界。relationship direct fact、naming rule 分组、pool 和 merger 已复用 dialect-aware canonical endpoint key；完整、排序后的 `conditions` 数组进入 candidate/observation/fingerprint identity，conditional summary 聚合全部 guard，并保留被 unguarded observation 覆盖的 guarded raw evidence。公开 `normalizedKey()` 继续承担 evidence reference/display 兼容职责。grouped evidence 只保留 deep-consensus attributes，冲突 provenance 留在 raw evidence。 |
| endpoint / metadata identity | `TableId`, `Endpoint`, `CanonicalIdentifierResolver`, `CanonicalEndpointKey`, `CanonicalEndpointKeyProvider`, `AliasSymbolTable`, `StatementParsePipeline`, metadata fact records | `phase-02`, `phase-06 Hard Boundaries` | catalog identity、naming、projection、metadata enhancement tests | MATCHED | 显式 catalog/schema 不被覆盖；bare table 只有在 production scan 已有唯一、规范的 scan/object namespace 时才物化到 endpoint。没有唯一上下文时保持 bare，且不按名称降级等价。物化 endpoint 与 canonical key 共同服务 metadata、DDL、SQL、profile 和 derived identity；跨 catalog 同名表负向测试覆盖 relationship、lineage 和 naming path。 |
| metadata inventory wire | `MetadataInventory`, `ScanResult`, `SourceCollectorPipeline`, `JsonResultWriter`, `MetadataIndexFact`, `MetadataIndexMemberFact` | semantic reader / metadata wire boundary | `MetadataInventoryOutputTest`, adaptor metadata tests, semantic reader inventory tests | MATCHED | direct与derived JSON输出同一scope、counts及table/column/constraint/index inventory；状态和流式writer边界已闭合。`members`按真实ordinal统一表达`FULL_COLUMN/PREFIX_COLUMN/EXPRESSION`及其交错顺序，旧`columns/expressions/subParts/seqInIndex`继续作为确定性兼容投影；无法证明顺序的旧mixed shape由core拒绝。 |
| file DDL metadata inventory | `DdlCatalogEvent`, `DdlCatalogEventAssembler`, `DdlMetadataInventoryAssembler`, `SourceCollectorPipeline` | semantic reader / file-only input boundary | DDL catalog assembler、final scan、sample-data result validator、semantic sample-data verifier tests | MATCHED | DDL文件只有显式声明`inventoryCoverage=COMPLETE_SCOPE`时，已校验typed table/column/FK/index事件才可组装为`status=COMPLETE, basis=DDL_DECLARATIONS`；普通`EVIDENCE_ONLY`不提升完整性。typed gap、identity冲突和普通声明解析失败都通过detached task outcome合并为coverage gap：混合成功/失败为PARTIAL，全失败且无事实为UNAVAILABLE，SQL查询解析失败不降级DDL inventory。串行和并行负向测试共用同一状态契约。 |
| semantic physical endpoint | `semantic.model.PhysicalEndpointRef`, `ingest.PhysicalEndpointJsonReader` | semantic ingest / evidence boundary | `PhysicalEndpointRefTest`, `PhysicalEndpointJsonReaderTest`, semantic normalization tests | MATCHED | table 和 column 使用不同构造入口；JSON 解析留在 ingest，中性模型不依赖 Jackson。catalog/schema 多段名称只在 column factory 的最后一个分隔点拆分，缺失列段明确失败。 |
| semantic CLI orchestration | `semantic.cli.Main`, command handlers, `SemanticKgFacade`, `SemanticExtractionFacade`, `SemanticNormalizationFacade` | semantic CLI build / extract / e2e flow | `SemanticCliIntegrationTest`, architecture tests | MATCHED | `Main`只解析、分发和渲染脱敏错误；CLI只调用三个core facade。build、extract与e2e通过`SemanticProcessingSession`复用input store→evidence store边界，handler不装配planner、session或artifact writer。 |
| semantic CLI error classification | `semantic.cli.Main`, semantic command handlers and exception types | semantic integration design | semantic CLI error tests | MATCHED | stderr 使用固定脱敏文案；参数、配置和 API key 缺失通过内部 usage 异常映射为 exit 2，wire validation、sharding、normalization、模型调用与 artifact I/O 失败映射为 runtime exit 1。 |
| semantic scan-result wire contract | `semantic.ingest.ScanResultContractValidator`, `semantic.ingest.ScanResultReader` | semantic ingest / evidence boundary | `ScanResultContractValidatorTest` | MATCHED | root、枚举和嵌套 evidence/warning 均严格校验；derived path 至少包含三个 endpoint，`pathLength == path.size()-1`，且 source/target 必须等于 path 两端。本行不代表metadata成员引用闭包。 |
| semantic metadata inventory reference closure | `MetadataInventoryClosureRules`, `ScanResultContractValidator`, `SemanticMetadataInventoryIndex`, `SemanticInputStore` | semantic reader / metadata inventory boundary | inventory reader、memory/disk parity tests | MATCHED | table/column/constraint/FK/index的identity、owner、cardinality和ordinal闭包由内存/磁盘reader共用规则验证；有序typed members覆盖mixed physical/expression完整交错顺序并校验每个物理成员列。`COMPLETE`仍只表示采集scope状态，缺少引用对象的scope会在正式semantic入口被拒绝。 |
| semantic disk-backed ingestion and catalog | `SemanticInputStore`, `SemanticInputWindowStore`, `SemanticEvidenceStore`, `SemanticEventContributionStore`, `SemanticEventAssociationStore`, `SemanticDiskBundleIndex`, `SemanticGlobalOwnerPlanner`, `SemanticKgStore`, `SemanticShardPlanner`, `DiskUnionFind`, `SemanticBoundedJsonReader`, `SemanticTokenEstimateBudget`, `SemanticFileTreeOperations` | semantic reader / evidence / sharding boundary | `SemanticInputStoreTest`, `SemanticEvidenceStoreTest`, `SemanticEventContributionStoreTest`, `SemanticEventAssociationStoreTest`, `SemanticEvidenceBundleSliceReaderTest`, `SemanticFileTreeOperationsTest`, `DiskUnionFindTest`, `SemanticPathBackedPipelineTest`, `SemanticDiskBackedMemoryGateTest` | MATCHED | 主scan reader、section spool、外排identity/offset索引、typed component、owner manifest与path-backed shard已实现。event contribution的标量、计数和成员分开落盘并外排归并，descriptor在成员列表与association key物化前执行base门限，association完成后再执行组合门限。standalone envelope以Jackson单字符串约束和有界writer共享累计码点预算，只有完整字段通过后才物化`JsonNode`。临时树使用不跟随符号链接的`walkFileTree`逐项清理，内存只随目录深度增长。 |
| semantic disk identity ordering | `Utf8ByteOrder`, `ExternalLineSorter`, `DiskSortedTextFile`, `DiskStringDictionary`, `SortedTextIndex` | semantic ingest / external index boundary | `Utf8DiskOrderingTest` | MATCHED | chunk排序、多路归并、整行查找和tab-key二分统一复用合法UTF-8的unsigned byte order。ASCII、组合字符、BMP私用区和补充平面字符正负向测试证明写入顺序与查找顺序一致；ASCII artifact identity不变。 |
| bounded `ScanBundle` construction | `ScanBundle`, `ScanMetadataInventory`, `SemanticInputWindowStore`, `SemanticEvidenceWindowProjector` | semantic ingest / bounded transport boundary | `ScanBundleInventoryContractTest`及生产facade tests | MATCHED | `ScanBundle`只保留要求显式`ScanMetadataInventory`的完整构造器，生产链由`SemanticInputWindowStore`传入已验证inventory。生产代码不再提供空`COMPLETE/LIVE_METADATA`伪造入口；测试使用test-only typed inventory fixture显式声明表列。 |
| semantic formal normalization | `SemanticExtractionDocumentNormalizer`, `SemanticSectionNormalizer`, `SemanticReferenceIndex`, `SemanticPhysicalReferenceIndex`, `SemanticOwnerIdRegistry`, `SemanticGraphAssembler`, `SemanticCanonicalIdentity` | semantic enricher / integration design | semantic normalization and graph assembler tests | MATCHED | evidence、物理endpoint、跨section owner ID和graph identity闭包已实现。显式 typed ref 存在时必须解析成功，每个 event display input/output 分别校验；review target先规范化section再按section解析，自动review ID只使用`targetSection + targetRef + type`。 |
| semantic offline KG assembly | `SemanticEvidenceStore`, `SemanticKgStore`, `SemanticKgArtifactWriter`, `SemanticKgArtifactPublisher`, `SemanticReferenceClosureStore` | semantic evidence / KG store boundary | disk-backed KG、external record conflict、cross-file closure与目录事务tests | MATCHED | 生产KG逐条读取全局evidence records并流式写wire v2；非diagnostic fact/event、endpoint node和edge必须具有非空可解析evidence，冲突ID会使逻辑build失败。FULL与DIGEST_ONLY都先在目标同级隐藏staging完成全部文件、摘要和闭包，再以同文件系统atomic move一次发布；晚期失败清理staging且不产生目标，已有目标保持不变。 |
| semantic event candidate classification | `SemanticEventExtractor`, `TypedSemanticEventClassifier` | semantic evidence builder / extraction design | semantic event、PostgreSQL routine identity tests | MATCHED | direct non-control write lineage 是唯一 event producer，derived 只作支持；source/operation 只按 typed `sourceObjectType` 与 `mappingKind` 精确映射。routine key/stable ID使用精确`FUNCTION/PROCEDURE/PACKAGE/PACKAGE_BODY/EVENT`类型和`sourceObjectIdentity`，同一routine的多个mapping仍聚合。PostgreSQL full/live以输入参数类型签名区分overload；纯`OUT`、参数名和默认值不进入签名。compact token-event以typed kind/name和声明statement identity隔离overload，不复制完整类型grammar。缺失值稳定降级为`SQL_WRITE/WRITE`，event kind固定为`SQL_WRITE_OPERATION`；路径、source前缀、表列名和evidence detail不参与结构分类。formal normalization的缺省event ID同样使用完整`eventCandidateRef`生成稳定ID。 |
| semantic event candidate identity | `SemanticEventExtractor`, `StableSemanticId` | semantic evidence builder | event identity/collision tests | MATCHED | routine、trigger和普通SQL-write event的分组key与ID都使用长度分隔的完整typed identity；不同source identity不会因显示slug相同而碰撞，`SemanticFactIds.slug()`不再参与event身份。 |
| semantic complete input closure | `SemanticEvidenceStore`, `SemanticDiskBundleIndex`, `SemanticPhysicalReferenceIndex`, `SemanticShardPlanner` | semantic extraction prompt input | complete-bundle、configuration、CLI rejection与shard closure tests | MATCHED | `SemanticEvidenceWindowProjector`只投影有界运输窗口；全局store归并后，正式抽取始终保留全部direct/derived facts、deterministic candidates、endpoint tables和可解析evidence refs。不存在focus或分片前事实数量裁剪，旧CLI/YAML字段明确拒绝，只有typed sharding可控制模型上下文。 |
| semantic deterministic candidates | `SemanticEvidenceStore`, `SemanticEventContributionStore`, `TripletCandidateBuilder`, `ReviewItemCandidateGenerator` | semantic evidence builder / extraction bundle | bundle candidate tests | MATCHED | 全局store在所有运输窗口上按完整identity聚合typed event，并保留全部fact/event/triplet/review candidates；名称驱动的`METRIC_SOURCE`和review limit分支已删除。正式metric只能由后续有证据的semantic normalization产生。 |
| semantic model request/response budget | `SemanticShardPlanner`, `SemanticPromptBudgetEstimator`, `SemanticRunPlan`, `SemanticRunArtifactWriter`, `SemanticCodexSessionCompletionService` | semantic extraction sharding / reconciliation | shard、reconciliation和Codex completion budget tests | MATCHED | 每个shard的完整owned/overlap prompt和仅含冲突变体的reconciliation prompt都执行保守输入门限；API调用继续使用配置输出门限。Codex request index v2和run manifest持久化`shardMaxOutputTokens/reconciliationMaxOutputTokens`，completion在物化外部shard/reconciliation JSON前分别执行对应门限；v1仍可重建证据包但不能完成正式run。 |
| semantic extraction artifact transaction | `SemanticRunArtifactWriter`, `SemanticRunManifestFactory`, `RunArtifactPublisher` | semantic extraction artifact boundary | run artifact、failure、retention与hash tests | MATCHED | staging创建后先原子写`IN_PROGRESS`，payload完成后再以原子manifest替换记录模式终态并发布。普通失败写`FAILED`；若终态写入自身失败，最后一个可解析`IN_PROGRESS`保留，且任何未完成run都不发布。 |
| semantic failed-run shard audit trail | `SemanticExtractionFacade`, `SemanticRunArtifactWriter`, `SemanticRunAuditArtifactWriter` | semantic extraction failure audit boundary | shard/reconciliation/final failure artifact tests | MATCHED | path-backed plan在同一staging事务内逐片归一化并原子落盘；后续失败保留已完成分片和hash但不发布run。`final-only`成功时才构建并发布裁剪候选。 |
| semantic reconciliation ownership | `SemanticRunArtifactWriter`, `SemanticReconciliationPromptBuilder`, `SemanticResultSelection`, `SemanticShardOutputOwnershipValidator` | semantic extraction reconciliation boundary | reconciliation and ownership tests | MATCHED | shard模型对象要求owned grounding；reconciliation只接受冲突`resolutions`和既有对象`renames`，不能新增对象、relation、fact或evidence，因此不产生新的owner归属问题。 |
| semantic reader/graph state isolation | `ScanBundle`, typed `Scan*Fact`, `EvidenceGraph`, `EvidenceGraphFact` | semantic reader/evidence graph | reader/graph mutation tests | MATCHED | 外层集合不可修改，typed fact `document()`、graph fact payload与diagnostics在构造和公开读取边界均deep-copy；调用方不能经公开`JsonNode`引用回写已验证状态。 |
| semantic standalone owner validation | `SemanticNormalizeExtractionCommandHandler`, `SemanticExtractionDocumentNormalizer`, `SemanticShardOutputOwnershipValidator` | semantic extraction owner boundary | owner与standalone normalization tests | MATCHED | 自动分片与独立normalize共用owner-aware入口；bundle必须携带合法`shardContext`，owned/overlap集合唯一、互斥且存在，模型对象必须有当前片owned grounding，overlap-only或越界引用原子失败。 |
| semantic governance default | `SemanticReferenceValidator`, `SemanticExtractionDocumentNormalizer`, semantic DTOs | semantic extraction governance boundary | normalization/review-status tests | MATCHED | 模型写入`BUSINESS_APPROVED`失败；正式semantic对象缺失状态归一为`SYSTEM_PROPOSED`，review item归一为`REVIEW_NEEDED`，backfill后不再留下空审核状态。 |
| MySQL metadata inventory | `MySqlMetadataCollector`, four `MySql*MetadataReader`, `MySqlMetadataReaderSupport` | adaptor live metadata boundary | `MySqlMetadataCollectorFactsTest` | MATCHED | collector/readers职责、partial-success、物理成员ordinal和`SUB_PART`采集已闭环。MySQL 8读取`EXPRESSION/IS_VISIBLE`，MySQL 5.7在字段不可用时使用兼容查询；prefix/组合/expression唯一性解释由统一`IndexEvidencePolicy`闭合，mixed physical/expression的完整typed member顺序进入公共metadata wire。 |
| Data Lineage extraction | `core.lineage.StructuredDataLineageExtractor`, `ProjectionTraceResolver`, `core.lineage.model.*`, `DataLineageMerger` | `phase-06 Data Lineage v1`, `sql-lineage-resolver.md` | `ProjectionTraceResolverTest`, `DataLineageMergerTest`, lineage golden | DOC_UPDATED | 正式链路只消费 structured events。statement source namespace进入 alias/known-physical 解析；`DataLineageMerger` 按 dialect-aware canonical source/target identity 去重，而输出 source 顺序仍按公开 key 稳定排序以保持 JSON 兼容。 |
| derived path inference | `core.derived.DerivedPathInferenceService`, `core.derived.DerivedPathGraphBuilder`, `core.derived.DerivedResultSelector`, `contracts.model.DerivedPathCandidate`, `JsonResultWriter` | `phase-06 Derived Path Evidence`, `phase-08 output` | `DerivedPathInferenceServiceTest`, `JsonResultWriterEvidenceOutputTest`, `ParserConfigRemovalTest` | MATCHED | 路径方向、canonical identity、edge variant、循环防护、table bridge和`minConfidence`过滤均已闭环。`maxFacts`由无状态selector在最终合并后按relationship、lineage、naming稳定顺序限制三类derived事实总数；低分path不占配额，裁剪会同步移除或重写可选naming引用，`0`表示不限制。 |
| DDL parser selection | `core.parser.runtime.DdlRelationParserRunner` | `phase-06 DDL token-event / full-grammar DDL 链路` | `DdlRelationParserRunnerTest`, `CorrectnessFixtureRunnerTest`, `CliEndToEndGoldenTest` | MATCHED | DDL 使用同一 `parser.mode` 策略；普通 parser runtime failure 可记录固定 fallback warning 并继续，structured result 契约违约直接失败。 |
| DDL relationship extraction | `core.relation.DdlRelationExtractionVisitor`, `core.naming.NamingEvidenceExtractor` | `phase-06 DDL Relationship 抽取` | `PostgresDdlParserTest`, `MySqlDdlParserTest`, DDL correctness fixtures | DOC_UPDATED | DDL parser 产 `DDL_FOREIGN_KEY` / `DDL_INDEX` / `DDL_COLUMN`；relationship 转换集中在 visitor，column inventory 进入 naming pool。endpoint pair 只执行一次命名匹配，但双方所有 DDL/metadata/structural observations 都合并为 raw provenance，精确重复用 `occurrenceCount` 折叠。 |
| adaptor live capabilities | `AdaptorContractValidator`, `ValidatedDatabaseAdaptor`, `AdaptorContractException`, grouped `collectors()/parsers()/profiling()`, `ScanCapabilityValidator`, `DatabaseAdaptor.resolveLiveScope` | `phase-03 Adaptor API 和 SPI`, adaptor phase docs | `AdaptorContractValidatorTest`, `ScanCapabilityValidatorTest`, `AdaptorApiVersionTest`, capability/live collector tests | MATCHED | JDBC前一次性读取并校验SPI/id/database types/capabilities/identifier rules/grouped records；`AdaptorCollectors`保留null member供core拒绝，集合与快照不再重复读取，null scope同样成为`AdaptorContractException/ADAPTOR_ERROR`。 |
| non-profile adaptor SPI result contract | `AdaptorResultContractValidator`, `AdaptorResultDetachmentSupport`, `SourceCollectorPipeline`, fallback `SqlRelationParser` | `phase-03 SPI 返回值信任边界` | `AdaptorResultContractValidatorTest`, `LiveSourceFailureBoundaryTest`, `SqlRelationParserRunnerTest` | MATCHED | metadata、object 和 database-DDL 先 detached copy，再校验 inventory、endpoint、evidence family/source、definition identity 和 warning envelope；全批成功后才写入 scan。fallback relationship candidate 同时验证 endpoint、family、source type、score，以及 evidence `source` 与输入 statement 的规范 source name；跨 source 注入会原子失败。 |
| remaining adaptor SPI result boundaries | `StructuredSqlParseExecutor`, `AdaptorParseResultContractValidator`, `SqlLogExtractor`, `DialectScriptFramer`, `StructuredSqlParser`, `StructuredDdlParser`, `ScriptFileExtractor`, `SqlRelationParserRunner`, `DdlRelationParserRunner`, `ParserBundleSelector` | `phase-03 SPI 返回值信任边界`, `phase-06 SQL/DDL 调用链` | `AdaptorParseResultContractValidatorTest`, `StructuredSqlRelationshipParserContractTest`, `StatementExecutionServiceProvenanceTest`, `ScriptFileExtractorContractTest`, `SourceCollectorPipelineLogContractTest`, runner/selector contract tests | MATCHED | framer/log/runner的statement/event/provenance/attribute/warning先全批校验再提交，null collection/element会被拒绝。selector只捕获外部parser调用的普通runtime failure；result shape检查与selection attribute装配在catch之外，null result/null attributes不会被fallback掩盖。 |
| public evidence source portability | `ScanInputPathResolver`, `ScriptFileExtractor`, `SourceNameNormalizer`, `JsonResultWriter` | `phase-08 output` | output portability与verification audits | MATCHED | 工作区文件使用相对路径；工作区外文件使用`external/sha256-<完整摘要>/<文件名>`，读取竞态或失败使用`external/unavailable/<文件名>`。script、DDL/object、四方言log、statement ID与warning共用该source，不输出本机绝对路径。 |
| evidence weight adjustment | `EvidenceWeightAdjustmentService`, `EvidenceWeightAdjuster`, `Evidence` | `phase-03 权重修正接口` | `EvidenceWeightAdjustmentServiceTest`, `EvidenceEnhancementPipelineTest` | MATCHED | hook 接收 deep-detached evidence 和 deep-immutable context options；返回结果相对 detached baseline 只允许修改 `[-1,1]` 内 score。core 用 baseline identity/attributes 与新 score 重建 evidence，relationship、naming 和 warning 只有整批成功后才替换，嵌套容器不能保留插件引用。 |
| built-in dialect weight policy | four built-in `DatabaseAdaptor.profiling().evidenceWeightAdjuster()` implementations | `phase-03 权重修正接口`, adaptor phase docs | adaptor capability / adjustment tests | MATCHED | 四个内置 adaptor 保留 SPI v6 要求的 identity hook，但不声明`EVIDENCE_WEIGHT_ADJUSTMENT`。core只在 adaptor 明确声明能力时调用hook，避免把无策略的占位实现误报为实际能力。 |
| live database-DDL candidate qualification | `StatementParsePipeline.qualifyDatabaseDdlCandidate` | `phase-02 Endpoint / RelationshipCandidate` | `StatementParsePipelineCandidateStateTest`, `ScanEngineDatabaseDdlSourceTest` | MATCHED | namespace qualification 只替换 endpoint，完整复制 confidence、evidence、rawEvidence、warnings 和 candidate-level attributes；直接 API 测试锁定状态保真。 |
| live conditional/index evidence | MySQL `MySqlIndexMetadataReader`, PostgreSQL `PostgresMetadataCollector.collectIndexes`, SQL Server `SqlServerMetadataCollector.collectIndexes`, `IndexEvidencePolicy` | adaptor phase docs, `phase-07` | live metadata/index tests | MATCHED | live policy同时校验visibility、expression、真实ordinal与MySQL `subParts`。只有可见、无表达式、无前缀的单物理列unique/PK证明`TARGET_UNIQUE`；普通、组合或prefix index仅允许真实首个物理成员支持lookup，expression-leading和invisible index不提供物理列证据。 |
| MySQL DDL prefix-index semantics | `MySqlRelationSql.g4`, `MySqlIndexSemantics`, token-event及v5.7/v8.0 typed DDL collectors | `phase-04`, `phase-06 DDL index evidence` | MySQL token/full exact parity tests | MATCHED | 三套parser分别从自身typed context映射`FULL_COLUMN/PREFIX_COLUMN/EXPRESSION`；table-level unique constraint与standalone index均保留prefix length。单个完整列unique才产生`TARGET_UNIQUE`；prefix/组合/expression unique不证明成员单列唯一，首个物理完整列或前缀列仍可产生`SOURCE_INDEX`，expression-leading不产生lookup。 |
| metadata/profile direction gates | `MetadataEvidenceEnhancer`, `DataProfileCandidateGenerator`, `IndexEvidencePolicy` | `phase-02`, `phase-07` | metadata/profile candidate tests | MATCHED | inventory/type gate、候选排序去重和配额继续生效；统一index policy在metadata enhancement和profiling lookup之前区分完整列唯一与首列lookup，MySQL prefix/组合/expression索引不能再经`TARGET_UNIQUE`误导方向或naming-discovery资格。 |
| closed semantic ordering / absence contract | `DataProfileCandidateGenerator.ProfileCandidatePriority`, `LineageTransformClassifier.TransformPrecedence`, `RelationshipSubtypeResolver.SubtypePrecedence`, semantic CLI `SemanticCliExitCode`, PostgreSQL set projection layout | enum reference、code implementation guide | `RepositorySemanticNumberContractTest`, `SemanticNumberArchitectureTest`及对应行为测试 | MATCHED | 固定语义类别、优先级、主导顺序和内部CLI结果均由enum表达，进程边界才转为int；项目自定义整数缺失使用`OptionalInt`。数量、阈值、位置、版本及外部协议code继续保持数值，标准库`indexOf/read`的既有`-1`协议不被重写。 |
| profiler diagnostics | `JdbcDataProfilerTemplate`, `JdbcExceptionClassifier`, `LiveDiagnosticSanitizer`, `ProfileOutcomeContractValidator`, `DataProfilePipeline` | `phase-07 安全与权限` | profile outcome/pipeline contract 和 dialect profiler tests | MATCHED | 外部 outcome 被视为不可信输入：status、evidence allowlist/source type、负向 policy 与 warning 状态契约先原子验证；plugin warning message/source/attributes 不进入结果，core 只按 status、adaptor id 和 endpoints 重建固定脱敏 warning。真实 driver/version smoke 是独立环境边界。 |
| profiler request/result isolation | `DataProfilePipeline`, `ProfileOutcomeContractValidator.NegativeProfileEligibility`, `ProfileRequest`, `ProfileOutcome`, `AdaptorResultDetachmentSupport` | `phase-07 外部 SPI 输入边界` | `DataProfilePipelineTest` | MATCHED | core在插件调用前从原scan candidate计算不可变negative eligibility；插件仍只接收深度隔离request，注入FK或删除conditional guard既不能回写scan，也不能改变负向资格。返回evidence继续深度隔离并全批延迟提交。 |
| profiler contract error category | `ProfileOutcomeContractValidator`, `SingleScanRunner`, `BatchScheduler` | `phase-03 DataProfiler trust boundary`, `phase-08 error code` | `DataProfilePipelineTest`, `CliErrorCodeContractTest`, `BatchCommandIntegrationTest`, `BatchSchedulerTest` | MATCHED | profile outcome 的 status、evidence family/source 和 negative-policy 违约统一抛 `AdaptorContractException`；direct API 原样抛出，single CLI 映射为 `ADAPTOR_ERROR`，batch case 保留同一 code 且整体仍为 `BATCH_PARTIAL_FAILURE`。错误文本固定脱敏，最后一个 outcome 失败也不会提交前序 profiling 状态。 |
| live collector diagnostics | `AdaptorResultContractValidator`, `LiveDiagnosticSanitizer`, `JdbcExceptionClassifier`, `SourceCollectorPipeline`, dialect live collectors | `phase-08 Warning 设计`, `phase-03 SPI 信任边界` | `AdaptorResultContractValidatorTest`, `LiveSourceFailureBoundaryTest`, scan/collector diagnostics tests | MATCHED | 内置 exception 路径和外部 callback/snapshot warning 都经过 core 边界。外部 warning 只接受受限 type/severity/code 与安全属性；plugin message/source/line 被丢弃并由 core 按 operation 重建固定内容。null/blank definition、null element 和 null list继续输出安全 `DEFINITION_UNAVAILABLE`。真实 driver/version smoke 仍是独立环境边界。 |
| profiler metrics | `JdbcDataProfilerTemplate`, `DataProfileMetrics`, `DataProfileEvidenceBuilder`, `NegativeProfileEvidencePolicy`, `ProfileOutcomeContractValidator`, `DataProfilePipeline`, dialect `IdentifierQuoter` modes | `phase-07 指标` | evidence-builder、pipeline contract、dialect fake-JDBC、profile-only catalog tests | MATCHED | 内置 live query 独立测量 source non-null rows、source distinct、matched distinct 和 target distinct。core consumer先验证全部 bounded outcomes，再统一应用结果；任一 contract violation 不会留下前序 candidate/evidence/warning 的部分修改。 |
| runtime config validation | `ScanConfigurationValidator`, `NamingRuleSetResolver`, `ScanConfig.resolve`, `ResolvedScanConfig`, `ScanEngine.scan`, `ScanCapabilityValidator` | `phase-08 配置校验` | `ScanConfigurationValidatorTest`, `ResolvedScanConfigTest`, `RuntimeConfigurationCliTest`, `BatchCommandIntegrationTest`, capability tests | MATCHED | source、live/JDBC、parser、derived、execution/profile limit 和 confidence 在 capability/JDBC 前校验。core 统一解析 naming rule files、合并 system/file/inline typed rules并拒绝重复 ID；CLI 仅解析相对路径，parser compatibility view 只复制最终 typed rules，避免二次加载。 |
| offline profile options | `ScanYamlConfigDto.DataProfile`, `SimpleYamlConfigLoader` | `phase-07 live-only profiling` | `OfflineProfileConfigurationRemovalTest`, SPI architecture tests | MATCHED | runtime、SPI v6、示例和证据已删除 offline INSERT sample 选项与分支。YAML DTO 仅保留四个旧字段作拒绝哨兵，发现后返回 `CONFIG_FORMAT_ERROR`，不映射到 runtime 类型。 |
| confidence / merger / output | `RelationshipMerger`, `DataLineageMerger`, `NamingEvidenceMerger`, `ConfidenceCalculator`, `JsonResultWriter` | `phase-02`, `phase-08` | `ConfidenceScoringExamplesTest`, `RelationshipMergerEvidenceAggregationTest`, `DataLineageMergerTest`, `JsonResultWriterEvidenceOutputTest` | MATCHED | raw/grouped 模型、catalog 输出和 observation count 已落地。`includeWarnings=false` 形成完整隐藏视图：根、relationship 和 lineage warning 数组为空且 `summary.warningCount=0`；内部 `ScanResult` warning 与 CLI 退出判断不变，严格 semantic reader 可直接消费该 writer 输出。 |
| table output UX | `TableResultWriter`, `SingleScanRunner` | `phase-08 Table 输出` | `TableResultWriterTest`, `TableOutputCliTest` | MATCHED | writer 保留 merger 顺序，evidence type 按首次出现稳定去重，长 endpoint/evidence 完整输出；空关系仍输出统一 warning 摘要和全部根 warning 明细。CLI `--format table` 可覆盖 YAML 并写文件，`--direct-output` 仍仅支持 JSON。当前不探测终端宽度、不折行/截断，也不应用 JSON 的 evidence/warning/observation 隐藏开关。 |
| correctness fixture / generated reports | `CorrectnessFixtureRunnerTest`, `CorrectnessFixtureExecutor`, `FixtureInputLoader`, `FixtureExecutionEngine`, `GoldenAssertion`, `GoldenWriter`, `CorrectnessFixtureInventoryTest`, `CorrectnessSummaryGeneratorTest`, `DataLineageAuditGeneratorTest` | `test-assets-map`, `code-implementation-guide`, `phase-06 correctness / golden 验收链` | 同名测试 | MATCHED | correctness fixture 是主 golden；执行框架复用 production statement service、extractor 与 enhancement。方言/profile fixture 进入 production runner；common fixture 的 direct structured-parser overload 也通过同一 `StructuredSqlParseExecutor` trust boundary。generated summary/audit 只写 verification artifact；inventory 和 freshness 边界保持不变。 |
| release heavy-job isolation | `heavy-job-lock.sh`, `run-correctness-isolated.sh`, `run-sample-data-isolated.sh`, `verify-all.sh`, `verify-release.sh` | `phase-06 发布级 correctness / sample-data`, `build-and-test-performance` | `heavy-job-lock-test.sh`, isolated runner tests, `heavy-job-mutual-exclusion-test.sh`, `verify-all-heavy-job-lock-test.sh`, `verify-release-test.sh`, `verification-wiring-test.sh` | MATCHED | release、correctness 与 sample-data 默认共用一把 heavy-job lock。最外层 owner 覆盖 smoke、full correctness、sample-data 和 manifest；嵌套入口只借用匹配 token。不完整元数据 fail-closed，完整 dead owner 通过原子 quarantine 回收；INT/TERM 先终止活跃子进程组再释放 owner lock；发布 sample-data 默认 case parallelism 为 1。 |
| release large-result validation | `ReleaseVerificationMain`, `SampleDataResultValidator`, `ExternalCanonicalJsonFingerprinter`, `CanonicalObjectFieldSorter`, `VerificationManifestBuilder`, `run-release-verification-tool.sh`, `verify-all.sh` | `phase-06 发布级 sample-data`, `build-and-test-performance` | Java verification、64 MiB child-JVM、wide-object/file-descriptor、wiring/manifest tests | MATCHED | Java流式校验与外存fingerprint已实现，manifest只消费小型报告。canonical object字段归并采用固定32路扇入的多阶段外排；中间chunk只保存字段名和value spool区间。低文件描述符、每chunk一个字段、跨归并组重复键及历史SHA测试固定bounded descriptor与canonical byte兼容性。 |
| typed parser discriminator closure | `PredicateEvent.joinKind`, `WriteEvent.mappingKind`, `DdlEvent.role/kind`, `StructuredEventDiscriminatorValidator`, `RelationshipCandidateFactory`, `DdlCatalogEventAssembler` | parser SPI typed-event boundary | parser result contract、parser fallback与恶意discriminator tests | MATCHED | contracts定义predicate join、write mapping、DDL index role/kind闭集，SPI v6 record继续传递其精确枚举名称。core按event type验证合法组合，未知值或错配抛`AdaptorContractException`且不fallback；relationship和DDL组装使用精确枚举映射，不再使用substring分类。 |
| PostgreSQL versioned golden | `test-fixtures/correctness/postgres/v16|v17|v18`, PostgreSQL `FullGrammarDialectModule` | `phase-05 PostgreSQL versioned correctness golden`, `phase-06 PostgreSQL 版本化 correctness` | `CorrectnessFixtureRunnerTest` with version filters | DOC_UPDATED | root `postgres` 是历史兼容 baseline；`v16`、`v17`、`v18` 是严格 full-grammar version golden，不存在 `postgres/v1` 这个版本。 |
| MySQL versioned golden | `test-fixtures/correctness/mysql/v5_7|v8_0`, MySQL `FullGrammarDialectModule` | `phase-04 Correctness 与 golden 状态`, `phase-06 MySQL correctness 命名约定` | `CorrectnessFixtureRunnerTest` | DOC_UPDATED | root `mysql` 是 token-event baseline；`mysql/v5_7` 与 `mysql/v8_0` 分别是严格 MySQL 5.7 / 8.0 full-grammar version golden。 |
| Oracle versioned golden | `test-fixtures/correctness/oracle/v12c|v19c|v21c|v26ai`, Oracle `FullGrammarDialectModule` | `phase-09 Oracle adaptor`, `phase-06 Parser mode 和 profile 选择` | `CorrectnessFixtureRunnerTest` with Oracle sample-data fixtures, `OracleAdaptorParserTest`, `OracleParserArchitectureTest` | DOC_UPDATED | root `oracle` 是 token-event baseline；versioned directories 强制 Oracle full-grammar profile。当前 versioned outputs 是 `INCOMPLETE_VERSIONED` generated parser golden，不是 token-event bridge golden。 |
| SQL Server versioned golden | `test-fixtures/correctness/sqlserver/v2016|v2017|v2019|v2022|v2025`, SQL Server `FullGrammarDialectModule` | `phase-10 SQL Server adaptor`, `phase-06 Parser mode 和 profile 选择` | `CorrectnessFixtureRunnerTest`, `SqlServerParserArchitectureTest`, `SqlServerTokenEventParserTest` | DOC_UPDATED | root `sqlserver` 是 token-event baseline；versioned directories 强制 SQL Server full-grammar profile。当前 sample-data 使用保守 T-SQL 子集；2017 `STRING_AGG`、2022 `DATETRUNC` / `GENERATE_SERIES`、2025 `VECTOR(...)` 已有 grammar-level version boundary，更多 T-SQL family 仍是 backlog。 |
| 代码结构注释 | `**/package-info.java`, production class/method Javadocs | `phase-06 代码结构注释索引`, `code-implementation-guide` | `DialectGrammarArchitectureTest`, `SemanticDocumentationArchitectureTest` | MATCHED | 双语/结构门禁覆盖 `Executor/Runner/Scheduler/Loader/Normalizer/Dispatcher/Selector` 及既有编排后缀；`StructuredSqlRelationshipParser` 与共享 `StructuredSqlParseExecutor` 已按 facade/trust-boundary 职责说明输入、输出、上下游和禁止职责。门禁不能自动证明内容准确，仍需代码评审。 |
| parser语义类职责行数门禁 | parser visitor/collector及relation-detector Analyzer/Support/Extractor/Resolver/Merger/Framer/Facade | `phase-06 Visitor 与语义 helper 职责边界` | `DialectGrammarArchitectureTest` semantic/framer gates | MATCHED | 400/450、framer 200和planner 250门禁只覆盖代码中明确列出的parser/relation-detector suffix范围；record AST豁免无永久allowlist。 |
| 全仓重要编排类职责规模门禁 | relation-detector与semantic-layer全部手写生产Java | `phase-06 代码结构注释索引` | `DialectGrammarArchitectureTest.repositoryProductionResponsibilityClassesRemainFocused` | MATCHED | 全仓扫描`src/main/java`：Visitor/Collector上限400；Analyzer/Support/Extractor/Resolver/Merger/Framer/Facade/Store/Planner/Publisher/Fingerprinter/Canonicalizer/Handler/Writer/Validator/Builder/Service/Index/Loader/Executor/Runner/Scheduler/Assembler上限450，generated、顶层record DTO和`package-info`排除且无allowlist。input lifecycle/metadata index、result selection/validation/rendering、artifact transaction/manifest、JSON section rendering及canonical object sorting已按真实职责拆分，原public facade保持不变。 |
| repository documentation / reachability | `docs/design`, `docs/guides`, `docs/parser-audit`, `scripts/audit/audit-java-reachability.sh` | design index, guide and current-audit contracts | `RepositoryDocumentationContractTest`, `ProductionReachabilityAuditTest` | MATCHED | 当前 HEAD 只维护设计、运行指南和登记过的现行 parser 审计；历史计划与快照仅保留在 Git 历史，完整生成报告属于 verification artifact。`jdeps`门禁从CLI/verification main、ServiceLoader和contracts/SPI根遍历生产bytecode；generated parser只作为路径节点，只有手写production类型属于删除候选，因此测试/文档引用和互引死循环都不能让死代码存活。当前候选不可达数为0。 |

## 已知实现事实

- 生产 scan 的单条 SQL 由 `StatementExecutionService.executeSql(...)` 调用 `SqlRelationParserRunner.parseStructuredAndRelations(...)`，一次结构化解析后同时服务 relationship 与 Data Lineage。runner、direct statement overload 和 relationship facade 都先经过 `StructuredSqlParseExecutor`；全部source收集后，SQL naming rule只在scan-level `EvidenceEnhancementService`对尚未进入`RelationshipMerger`的candidate集合执行一次。parser mode、fallback warning、SPI isolation和diagnostics在production/direct链路中保持一致。
- `CorrectnessFixtureRunnerTest` 不再自己拼 SQL/DDL -> relationship/lineage/namingEvidence 流程；它通过 `FixtureExecutionEngine` 复用 `StatementExecutionService` 和 `EvidenceEnhancementService`。方言/profile SQL 与全部 DDL fixture 复用 runner contract；common SQL fixture 使用 direct structured-parser overload，但该 overload 与 production runner 共用同一 detached、全批 SQL result boundary。DDL fixture 不额外引入 scan-level metadata/naming enhancement。
- package/path、public类型/文件名、ServiceLoader描述符、旧目录命名和production reachability均由默认架构测试覆盖；可选`rg`只用于扩大人工审计，不替代门禁。
- `SqlGrammarProfileRegistry` 中的版本字符串解析会使用字符串/正则处理配置值；这不属于 SQL/DDL 结构解析。SQL/DDL relationship 和 lineage 不能依赖特殊表名/列名过滤。
- MySQL 5.7 / 8.0 full-grammar 当前已有独立 `test-fixtures/correctness/mysql/v5_7`、`test-fixtures/correctness/mysql/v8_0` versioned golden。root MySQL correctness 仍是 token-event baseline；三者都必须保持独立，不能互相覆盖。

## 剩余技术债

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| unaudited parser statement families | BACKLOG | 当前 natural corpus 与 semantic-equivalent benchmark 没有暴露未分类的 token/full gap；这不证明任一 parser 已覆盖官方语法全集。新 statement family 必须用同语义 SQL、exact observation 和版本边界证明差异后，才能分类为 typed-visitor gap；不能用数量或笼统“root 更弱”代替证据，也不能恢复 scanner 或名字过滤。 |
| Oracle 官方语法覆盖扩展 | BACKLOG | Oracle 已有 adaptor、profile 和 sample-data versioned full-grammar golden，但 full-grammar 仍是 `INCOMPLETE_VERSIONED`。后续需要按 Oracle SQL/PLSQL Reference 扩大 `.g4` / typed visitor 覆盖面，并补版本边界 fixture。 |
| production Javadoc / package documentation | MATCHED | compiler/doc-tree 门禁覆盖当前实际编排 role suffix、类与大型方法；warning 内容契约测试验证 `ProfileOutcome` 与 diagnostics 的敏感信息策略。门禁不能自动证明 prose 与调用链一致，内容准确性仍需代码评审。 |
| table identity 与 fact canonicalization | MATCHED | `TableId`、dialect canonical key、source namespace、live profile namespace 和 derived bridge 已使用完整身份；跨 catalog/schema、quoted case 和 profile-only mismatch 由 focused tests 保护。 |
| 直接 API 文件路径 | FOCUSED_GREEN | `ScanInputPathResolver` 是 `files + paths + include` 的唯一展开 owner；CLI 以配置文件父目录解析，core direct API 以显式或当前工作目录解析。missing、non-regular 和 unreadable 输入在 scan 前 fail-fast。 |
| relationship 条件与 provenance merge | MATCHED | 完整 guard identity、conditional/unconditional summary 与 polymorphic 计算已通过 focused tests；grouped evidence 顶层只保留所有 observation 一致的 consensus attributes。 |
| parser语义类职责行数门禁 | MATCHED | 400/450与script framer/planner门禁在已登记parser/relation-detector suffix范围内实现；top-level record豁免使用JDK compiler AST判断。 |
| 全仓重要编排类职责规模门禁 | MATCHED | relation-detector与semantic-layer目标职责后缀统一受400/450有效代码行门禁；当前超限store、artifact writer、output writer和fingerprinter均按生命周期、校验、事务、rendering或外排排序职责拆分，无永久allowlist、改名或空wrapper绕过。 |
| profiler warning 脱敏与权限分类 | MATCHED | 内置 profiler 与外部 SPI outcome 共用 core 安全边界；plugin warning 只参与 status/type/code 契约校验，message/source/attributes 被丢弃并由 core 重建固定 warning。 |
| live profiler 指标完整性 | MATCHED | 内置四项计数、方言合法table qualification与profile-only catalog校验已闭环；negative eligibility在插件调用前由core从原candidate固化，外部request突变不能影响declared-FK/physical/conditional policy。 |
| profiler request/result 深度隔离 | MATCHED | scan candidate与返回evidence的嵌套状态已隔离；调用前不可变policy snapshot进一步隔离了插件可变request和core-owned负向资格。 |
| profiler contract 错误类别 | MATCHED | `ProfileOutcomeContractValidator` 统一抛 `AdaptorContractException`；direct、single 和 batch 分别保持异常、`ADAPTOR_ERROR` 和 case 级 `ADAPTOR_ERROR`。 |
| derived `minConfidence` 阈值 | MATCHED | 未舍入衰减值低于阈值的 path 在配额和 evidence 聚合前过滤；等于阈值保留，最终 confidence 只做四位小数输出舍入。 |
| runtime 配置校验 | MATCHED | live/JDBC、source、parser、derived、execution/profile limit、confidence 与 naming rule files 均由 core 最终配置入口统一校验；direct API、CLI 和 batch 使用同一规则集。 |
| offline profile 配置 | MATCHED | runtime 和 SPI v6 已删除无 producer 的 offline INSERT sample 配置与 metrics 分支；YAML 旧字段仅作显式拒绝哨兵。 |
| profile candidate selection semantics | MATCHED | live metadata inventory/type、排序/去重、配额和声明FK豁免已实现；MySQL prefix/组合/expression索引通过统一policy只提供合法lookup，不再伪造单列唯一或方向gate。 |
| MySQL prefix index semantics | MATCHED | token-event、full v5.7、full v8.0和live metadata均保留完整列、前缀列与表达式成员边界；仅完整单列unique提供`TARGET_UNIQUE`，前缀首列可提供lookup但不证明完整列唯一。 |
| semantic metadata member reference closure | MATCHED | `members`保留每个完整列、前缀列和表达式的连续ordinal及交错顺序；共享closure rules验证shape、物理列引用和旧shape兼容边界。 |
| semantic large-input memory proof | MATCHED | 默认测试保留1 MiB/96 MiB smoke；`semantic-memory-gate`固定128 MiB/96 MiB并进入`verify-release.sh`，`semantic-memory-gate-extended`固定1 GiB/512 MiB按需执行。结构证据同时覆盖100,000节点parent链、association refs高扇出、跨transport window的大量同event contributions、64 MiB standalone envelope在32 MiB堆内提前拒绝，以及20,000路径逐项清理。门禁证明指定输入形状有界完成或有界拒绝，不代表吞吐承诺。 |
| MySQL live object parser completeness | MATCHED | `information_schema` 只用于枚举对象身份；完整 parser 输入来自 `SHOW CREATE PROCEDURE/FUNCTION/VIEW/TRIGGER/EVENT`，单对象失败产生安全 warning 并跳过。 |
| live database-DDL structural contract | MATCHED | Oracle DDL 使用 `DBMS_METADATA.GET_DDL`；SQL Server/PostgreSQL 明确输出关系解析用结构骨架，组合 FK 分别按 `constraint_column_id` / ordinality 配对并保留 catalog。完整可回放 DDL（type modifier、default、identity/generated/computed/collation）不属于当前骨架契约。 |
| live collector warning sanitization | MATCHED | metadata snapshot 与 object/database-DDL callback warning 都先进入临时结果，由 core 校验 envelope、丢弃 plugin 文本和位置并重建固定消息；任何非法 warning 使对应 SPI outcome 原子失败。真实驱动 smoke 保留为环境验收。 |
| live warning object context | MATCHED | record 存在但 body 为 null/blank、第三方 collector 返回 null 元素/null list，以及 MySQL `SHOW CREATE TABLE` / Oracle `DBMS_METADATA.GET_DDL` 成功但零行，均生成带对象身份的安全 `DEFINITION_UNAVAILABLE`，且不构造空 definition。 |
| observation summary count | MATCHED | relationship、lineage、naming 和 derived path 的 direct/derived summary 均通过统一 helper 累加 `occurrenceCount`；折叠 multiplicity 不参与 repeated-observation confidence。 |
| CLI error-code contract | MATCHED | `LiveSourceConfigurationException`在single/batch中保持`CONFIG_FORMAT_ERROR`，`AdaptorContractException`保持`ADAPTOR_ERROR`；MySQL catalog/schema canonicalization冲突已经使用typed live配置异常，并在连接或查询前失败。 |
| warning suppression wire contract | MATCHED | `includeWarnings=false` 同时清空根及 fact-level warning 数组并把可见 `warningCount` 置零；内部 warning 与 CLI 退出状态不被输出视图修改，writer/reader contract test 已覆盖。 |
| Oracle owner fail-fast | MATCHED | 显式 catalog 已拒绝，显式 schema、connection schema、metadata user 按顺序解析；三者都无法证明 owner 时在任何 `prepareStatement` 前抛出脱敏配置错误。 |
| correctness fixture execution identity | MATCHED | fixture-local 输入在相同执行配置下按 content hash 去重；correctness tree 外的 tracked sample-data 以规范 repo-relative source path 作为资产身份，即使当前内容相同也分别保护。不使用 allowlist。 |
| release heavy-job mutual exclusion | MATCHED | release、correctness 与 sample-data 的直接和嵌套入口共用 owner token；smoke 阶段也在锁内，并发首次抢锁、incomplete metadata、active/stale owner、borrower 和中断释放均有 shell contract test。 |
| non-profile adaptor SPI result contract | MATCHED | metadata/object/database-DDL/fallback relationship、log、framer和structured parser边界均原子校验；selector的fallback catch只覆盖外部parser调用，core装配发现的null result/null attributes保持`AdaptorContractException`。 |
| formal semantic default owner identity | MATCHED | shard canonicalization与formal normalization共用canonical identity；自动review先规范化section，再以`targetSection + targetRef + type`生成稳定ID，reason不参与identity。 |
| semantic reference closure | MATCHED | 显式typed ref必须成功解析，每个event display ref分别校验；review target按规范化section查询owner，不能被名称或其他section的同名ID掩盖。 |
| semantic reconciliation ownership | MATCHED | reconciliation只允许既有冲突variant选择和展示名称修改，不能新增对象或relation。 |
| semantic final-only failure retention | MATCHED | final-only从完整staging构建独立发布候选；晚期失败保留完整审计材料，成功发布后才尽力清理staging。 |
| release validation assertion scope | MATCHED | Java流式校验、summary/source-location语义及审计fingerprint已实现；canonical object field sorter以固定32路多阶段归并限制同时打开的chunk，并由超宽object、低文件描述符和历史SHA测试固定外存边界。 |
| evidence weight adjuster contract | MATCHED | core 向 hook 提供 deep-detached evidence 和 deep-immutable options，只接受 score 变化并从 baseline 重建返回值；嵌套突变、warning 副作用或最后一项违约均不会留下部分 scan 状态。 |
| built-in evidence weight policy | MATCHED | 内置adaptor保留identity hook但不声明能力；core只调用明确声明`EVIDENCE_WEIGHT_ADJUSTMENT`的外部实际策略。 |
| live database-DDL candidate state | MATCHED | namespace qualification 保留全部 candidate 状态，直接 API 测试覆盖 candidate-level attributes。 |
| SQL runner migration residue | MATCHED | no-op policy helper、误导 Javadoc 和 direct SQL execution 的无效 config overload 已删除；fallback parser 结果进入统一 adaptor contract 边界。 |
