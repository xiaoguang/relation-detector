# Common Natural Sample Data

This directory is the natural portable benchmark used by the sample-data CLI
comparison. It intentionally excludes parser coverage mirror bodies and
`*-for-golden.sql` files. Those live in `sample-data/common-parser-coverage`
and remain covered by correctness fixtures.
