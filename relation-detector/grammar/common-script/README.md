# Common Script Grammar

Owns `CommonScriptLexer.g4`. It frames portable semicolon-delimited client scripts and records statement ranges; it does not infer relationships, lineage, naming evidence, or DDL facts. Generated classes remain in `com.relationdetector.core.script`.
