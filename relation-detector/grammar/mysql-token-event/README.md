# MySQL Token-Event Grammar

Owns `MySqlRelationSql.g4`, the compact MySQL structural fallback. The entry rule is `script`; generated classes remain in `com.relationdetector.mysql.tokenevent` and are consumed by the MySQL adaptor. Version-strict syntax belongs in the versioned MySQL grammar modules.
