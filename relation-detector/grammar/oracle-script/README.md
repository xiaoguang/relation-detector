# Oracle Client Script Grammar

Owns `OracleClientScriptLexer.g4`. It frames Oracle client scripts, including standalone slash object terminators, while preserving source ranges. It does not create SQL facts.
