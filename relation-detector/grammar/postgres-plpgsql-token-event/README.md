# PostgreSQL Token-Event PL/pgSQL Grammar

This compact, version-neutral fallback recognizes the PL/pgSQL structures needed to expose static SQL to the PostgreSQL token-event parser. It does not claim version-strict coverage and must not load a versioned full grammar.
