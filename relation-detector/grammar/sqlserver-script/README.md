# SQL Server Client Script Grammar

Owns `SqlServerClientScriptLexer.g4`. It frames T-SQL client batches and recognizes standalone `GO` separators without interpreting strings or comments as separators. It does not produce facts.
