# Oracle Token-Event Grammar

Owns `OracleRelationSql.g4`, the compact Oracle structural fallback. The entry rule is `script`; generated classes remain in `com.relationdetector.oracle.tokenevent`. Version-strict Oracle syntax remains in the versioned grammar modules.
