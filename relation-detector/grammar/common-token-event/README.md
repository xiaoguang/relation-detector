# Common Token-Event Grammar

Owns `CommonRelationSql.g4`, the portable structural fallback used by core. The entry rule is `script`; generated classes remain in `com.relationdetector.core.antlr.common` and are consumed by the common structured parser. Dialect-only syntax belongs in adaptor-specific grammar modules.
