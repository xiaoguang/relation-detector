# PostgreSQL Token-Event Grammar

Owns `PostgresRelationSql.g4`, the compact PostgreSQL structural fallback. The entry rule is `script`; generated classes remain in `com.relationdetector.postgres.tokenevent`. PL/pgSQL procedural shells are owned by dedicated routine grammar modules.
