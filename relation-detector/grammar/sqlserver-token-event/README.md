# SQL Server Token-Event Grammar

Owns `SqlServerRelationSql.g4`, the compact T-SQL structural fallback. The entry rule is `tsql_file`; generated classes remain in `com.relationdetector.sqlserver.tokenevent`. Version-specific syntax remains in the versioned SQL Server grammar modules.
